@page
@model HuiNan2020OneClass.Pages.ClassNubers.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ClassNuber[0].ClassNuberName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ClassNuber[0].CreatTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ClassNuber[0].UpdateTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ClassNuber[0].CreatUserName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ClassNuber[0].IsDelete)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ClassNuber) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ClassNuberName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UpdateTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatUserName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsDelete)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
